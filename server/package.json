{
  "name": "conversation-api-serverless",
  "version": "2.1.0",
  "private": true,
  "scripts": {
    "test": "tsc --noEmit",
    "build": "tsc",
    "start": "npx twilio-run --functions-folder dist/functions --assets-folder dist/assets --port 3003"
  },
  "dependencies": {
    "@twilio-labs/serverless-runtime-types": "^4.0.1",
    "@twilio/runtime-handler": "2.1.0",
    "twilio": "5.8.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "twilio-run": "^4.2.0"
  },
  "engines": {
    "node": "20"
  }
}
